# Leave empty locally to use Vite proxy → backend :8000
# In production (Vercel / Cloudflare Pages) set full API origin, no trailing slash:
#   VITE_API_BASE=https://jp-learning.onrender.com
VITE_API_BASE=
